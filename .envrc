use flake

# default logging settings
export RUST_LOG=info,canvas_lms=debug,pigment=debug,ebauche=debug,ebac=debug,oil=debug

# ebauche settings
export EBAUCHE_DB=res/db

if [ -f .envrc.secret ]; then
    source .envrc.secret
fi
