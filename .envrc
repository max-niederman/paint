use flake

# default logging settings
export RUST_LOG=canvas_lms=debug,ebauche=debug,cpig=debug

# ebauche settings
export EBAUCHE_DB=res/db

if [ -f .envrc.secret ]; then
    source .envrc.secret
fi
