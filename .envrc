use flake

source_env_if_exists .envrc.local

# oil
export OIL_ADDR=0.0.0.0:4200
export OIL_MONGODB_URI="mongodb://root:paint@localhost:27017/"
export OIL_RES_DB=$(expand_path ./api/db)

# glaze
export VITE_OIL_URL=http://localhost:4200