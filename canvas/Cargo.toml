[package]
name = "canvas-lms"
version = "0.1.0"
edition = "2021"

[dependencies]
reqwest = "0.11"
chrono = { version = "0.4", features = ["serde"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[lib]
path = "src/lib.rs"
